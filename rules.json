{
  "rules": {
    "$provider": {
      "$user": {
        ".read": "auth.id == $user && auth.provider == $provider",
        ".write": "auth.id == $user && auth.provider == $provider",
      }
    }
  }
}